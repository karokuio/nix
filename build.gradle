apply plugin: 'groovy'
apply plugin: 'application'

mainClassName = 'nix.Application'

repositories {
  mavenLocal()
  jcenter()
}

dependencies {
    compile "io.karoku:pluto:0.1.0"

    testCompile "org.spockframework:spock-core:$spock_core"
    testCompile "junit:junit:$junit"
}
